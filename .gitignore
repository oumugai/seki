# Personal, machine-specific Claude Code permissions — not shared.
# (.claude/commands/ and .claude/skills/ ARE shared and tracked.)
.claude/settings.local.json

# Rust build artifacts. These were tracked by mistake until 0.5.1; they are
# regenerated by every build and made the repository ~350 MB.
/target
